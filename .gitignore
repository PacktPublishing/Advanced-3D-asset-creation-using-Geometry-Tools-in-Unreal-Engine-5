# Unreal Engine Generated Files
# These files are generated by the engine and can be recreated, so we don't need to track them
/Saved/
/Intermediate/
/DerivedDataCache/

# Build files
# Ignore build files as they are platform-specific and can be regenerated
/Build/

# Binary Files
# Binaries can be very large and are typically rebuilt from source
/Binaries/

# Plugins
# Ignore intermediate and binary files in plugins, but keep the source files
/Plugins/*/Intermediate/
/Plugins/*/Binaries/

# Content
# Ignore only specific content that's generated or not needed in version control
/Content/Collections/
/Content/Developers/

# Visual Studio files
# These files are user-specific or can be regenerated
.vs/
*.suo
*.opensdf
*.sdf
*.VC.db
*.VC.opendb
*.vsconfig

# Visual Studio Code files
# User-specific VSCode settings
.vscode/

# Rider files
# User-specific JetBrains Rider IDE files
.idea/
*.iml

# macOS system files
# Hidden files created by macOS that aren't needed
.DS_Store
.AppleDouble
.LSOverride

# Windows system files
# Windows-specific files that aren't needed
Thumbs.db
ehthumbs.db
Desktop.ini

# Log files
# Log files can be large and aren't typically needed in version control
*.log

# Cache files for the editor to use
# These can be large and are regenerated as needed
DerivedDataCache/*

# Crash dumps
# Crash dumps are typically large and contain sensitive information
*.dmp

# Configuration files that might contain sensitive information
# Be cautious with these - you might want to track some config files
/Config/DefaultEditorPerProjectUserSettings.ini

# Cooked assets
# Cooked assets are platform-specific and can be recreated
/Cooked/

# AutoScreenshot Plugin
# If you're using this plugin, these screenshots don't need to be in version control
/Saved/AutoScreenshot.png

# IOS deployment files
# These are specific to iOS deployment and can be regenerated
/Build/IOS/Resources/
*.mobileprovision

# Android deployment files
# These are specific to Android deployment and can be regenerated
/Build/Android/
*.keystore

# Ignore user-specific files
# These files often contain user-specific settings that shouldn't be shared
*.user
*.userosscache
*.sln.docstates

# Packages
# It's better to ignore package files and have them downloaded by developers as needed
*.7z
*.zip
*.rar

# Exclude specific file types that are generated or not needed
# These are typically large files that can be regenerated or aren't needed in version control
*.pdb
*.mdb